{
  "name": "cargaison",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "tsc",
    "dev": "concurrently \"npm run build:watch\" \"php -S localhost:8000 -t public\"",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "json-server": "json-server db.json --port 3000",
    "dev:full": "concurrently \"npm run json-server\" \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "cargaison",
    "logistics"
  ],
  "author": "",
  "license": "ISC",
  "description": "Système de gestion de cargaisons",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "express": "^4.21.2",
    "json-server": "^1.0.0-beta.3"
  }
}
