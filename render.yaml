services:
  - type: web
    name: cargaison
    env: php
    buildCommand: composer install
    startCommand: vendor/bin/heroku-php-apache2 public/
    envVars:
      - key: NODE_ENV
        value: production
    buildFilter:
      paths:
        - public/**
        - src/**
        - composer.json
        - composer.lock